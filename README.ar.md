# 🎉 مشروع Codeprops - دليل التشغيل

## 📋 **نظرة عامة**

تم إنشاء موقع ويب ثابت متعدد اللغات احترافي باستخدام أحدث التقنيات.

### ✨ **المميزات الرئيسية**

1. **🌍 دعم 3 لغات:**
   - الإنجليزية (`/en`)
   - الألمانية (`/de`)
   - العربية (`/ar`) مع دعم RTL

2. **⚡ أداء فائق:**
   - تصدير ثابت 100% (Static Export)
   - لا يحتاج سيرفر
   - حجم أول تحميل: **102 كيلوبايت فقط**

3. **🎨 تصميم احترافي:**
   - واجهة مستخدم حديثة
   - متجاوب مع جميع الأجهزة
   - رسوم متحركة سلسة

4. **🔍 محسّن لمحركات البحث (SEO):**
   - Sitemap تلقائي
   - Structured Data
   - OpenGraph Cards
   - hreflang tags

---

## 🚀 **التشغيل السريع**

### 1. تشغيل السيرفر المحلي

```bash
npm run dev
```

بعدها افتح المتصفح على: `http://localhost:3000`

### 2. بناء الموقع للنشر

```bash
npm run build
```

الملفات الجاهزة ستكون في مجلد `/out`

### 3. اختبار البناء النهائي

```bash
npm run start
```

---

## 📁 **هيكل المشروع**

```
codeProps_new/
├── app/
│   ├── [locale]/          # صفحات اللغات
│   │   ├── layout.tsx     # القالب الرئيسي
│   │   ├── page.tsx       # الصفحة الرئيسية
│   │   ├── about/         # صفحة من نحن (قريباً)
│   │   ├── services/      # صفحات الخدمات (قريباً)
│   │   └── contact/       # صفحة التواصل (قريباً)
│   ├── globals.css        # الأنماط العامة
│   ├── sitemap.ts         # خريطة الموقع
│   └── robots.ts          # ملف الروبوتات
├── components/
│   ├── ui/                # مكونات الواجهة
│   └── layout/            # Header و Footer
├── lib/
│   ├── i18n.ts            # إعدادات اللغات
│   ├── translations.ts    # الترجمات
│   └── seo.ts             # إعدادات SEO
├── messages/              # ملفات الترجمة
│   ├── en.json           # الإنجليزية
│   ├── de.json           # الألمانية
│   └── ar.json           # العربية
└── public/               # الملفات العامة
```

---

## 🌐 **إضافة لغة جديدة**

### الخطوات:

1. **أضف اللغة في `lib/i18n.ts`:**
```typescript
export const locales = ['en', 'de', 'ar', 'fr'] as const;
```

2. **أنشئ ملف ترجمة جديد:**
```
messages/fr.json
```

3. **أضف اسم اللغة:**
```typescript
export const localeNames: Record<Locale, string> = {
  en: 'English',
  de: 'Deutsch',
  ar: 'العربية',
  fr: 'Français',
};
```

4. **أعد البناء:**
```bash
npm run build
```

---

## ✏️ **تعديل المحتوى**

### تعديل النصوص:

افتح ملفات الترجمة في مجلد `messages/`:
- `en.json` - للإنجليزية
- `de.json` - للألمانية
- `ar.json` - للعربية

### مثال:

```json
{
  "hero": {
    "title": "العنوان الجديد",
    "subtitle": "العنوان الفرعي",
    "cta": "ابدأ الآن"
  }
}
```

---

## 🎨 **تخصيص التصميم**

### الألوان:

عدّل `tailwind.config.ts`:

```typescript
colors: {
  primary: {
    500: "#0ea5e9",  // اللون الأساسي
    // ... ألوان أخرى
  }
}
```

### الخطوط:

عدّل في `app/[locale]/layout.tsx`:

```typescript
const fontSans = Inter({ subsets: ["latin"] });
const fontHeading = Outfit({ subsets: ["latin"] });
```

---

## 📤 **النشر (Deployment)**

### Vercel (الأسهل):

```bash
npm install -g vercel
vercel --prod
```

### Netlify:

```bash
npm run build
netlify deploy --prod --dir=out
```

### أي استضافة ثابتة:

1. شغّل: `npm run build`
2. ارفع محتويات مجلد `/out` للسيرفر
3. تأكد أن السيرفر يدعم Static Files

---

## 🔧 **المتغيرات البيئية**

أنشئ ملف `.env.local`:

```env
NEXT_PUBLIC_SITE_URL=https://codeprops.com
NEXT_PUBLIC_SITE_NAME=Codeprops
NEXT_PUBLIC_DEFAULT_LOCALE=en
```

---

## ✅ **ما تم إنجازه**

- ✅ البنية الأساسية للمشروع
- ✅ دعم 3 لغات مع RTL
- ✅ Header و Footer متجاوبين
- ✅ الصفحة الرئيسية بكامل الأقسام
- ✅ SEO محسّن بالكامل
- ✅ Sitemap و Robots.txt
- ✅ تصدير ثابت يعمل 100%
- ✅ تصميم احترافي مع Tailwind

---

## 📋 **ما يحتاج إكمال**

### قريباً:
- ⏳ صفحة "من نحن"
- ⏳ صفحات الخدمات
- ⏳ صفحة الأعمال (Portfolio)
- ⏳ صفحة المدونة
- ⏳ نموذج التواصل
- ⏳ رسوم متحركة متقدمة
- ⏳ نظام المدونة بـ MDX

---

## 🆘 **حل المشاكل الشائعة**

### المشكلة: لا يعمل البناء
```bash
# احذف المجلدات المؤقتة
rm -rf .next out node_modules
npm install
npm run build
```

### المشكلة: أخطاء TypeScript
```bash
npm run type-check
```

### المشكلة: مشاكل في الأنماط
```bash
# تأكد من تثبيت Tailwind
npm install -D tailwindcss postcss autoprefixer
```

---

## 📞 **الدعم**

للمساعدة أو الأسئلة:
- **البريد:** info@codeprops.com
- **الهاتف:** +1 (555) 123-4567

---

## 📈 **الأداء الحالي**

- ✅ **First Load JS:** 102 KB
- ✅ **Build Time:** ~10 ثوانٍ
- ✅ **Static Pages:** 9 صفحات
- ✅ **Languages:** 3 لغات

---

**حالة المشروع:** 🟢 **جاهز للتطوير**

آخر تحديث: 2026-01-06

---

## 💡 **نصائح مهمة**

1. **دائماً اختبر قبل النشر:**
   ```bash
   npm run build
   ```

2. **راجع الترجمات:**
   تأكد من ترجمة كل النصوص في ملفات `messages/`

3. **استخدم Git:**
   ```bash
   git add .
   git commit -m "وصف التعديلات"
   ```

4. **حافظ على الأداء:**
   - تجنب المكتبات الثقيلة
   - استخدم lazy loading للصور
   - اختبر على 3G

---

بالتوفيق! 🚀
